name: Test Workflow

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Show folder structure
        run: |
          echo "=== ROOT ==="
          ls -lah
          echo "=== WRAPPER ==="
          ls -lah wrapper || true

      - name: Test success
        run: echo "WORKFLOW IJO 🔥"
