name: GCam Go Clone & Patch
on:
  workflow_dispatch:
    inputs:
      mod_name:
        description: 'Nama Mod Lu'
        required: true
        default: 'GCam Go Drpto Mod'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Your Repo
        uses: actions/checkout@v4

      - name: Setup Tools
        run: |
          sudo apt-get update
          mkdir -p bin input output
          wget https://github.com/iBotPeaches/Apktool/releases/download/v2.10.0/apktool_2.10.0.jar -O bin/apktool.jar
          echo "java -jar $GITHUB_WORKSPACE/bin/apktool.jar \$@" > bin/apktool
          chmod +x bin/apktool
          echo "$GITHUB_WORKSPACE/bin" >> $GITHUB_PATH

      - name: Clone Drpto Repo (Narik APK)
        run: |
          # Kita clone reponya tipis-tipis aja (depth 1) biar gak lama
          git clone --depth 1 https://github.com/Drpto/packages_apps_prebuilt-camera.git drpto_repo
          # Cari file APK di dalem folder GoogleCameraGo (biasanya lokasinya di situ)
          find drpto_repo -name "*.apk" -exec cp {} input/base.apk \;
          echo "APK berhasil diculik dari repo Drpto!"

      - name: Decompile & Patching
        run: |
          apktool d input/base.apk -o workdir
          cat << 'EOF' > patcher.py
          import os, re
          def patch():
              # Sodok Aux di semua smali
              for i in range(1, 12):
                  s_dir = f"workdir/smali_classes{i}" if i > 1 else "workdir/smali"
                  if not os.path.exists(s_dir): continue
                  for root, _, files in os.walk(s_dir):
                      for file in files:
                          if file.endswith(".smali"):
                              p = os.path.join(root, file)
                              with open(p, "r", encoding="utf-8") as f:
                                  c = f.read()
                              if "const/4 v0, 0x2" in c:
                                  with open(p, "w", encoding="utf-8") as f:
                                      f.write(c.replace("const/4 v0, 0x2", "const/4 v0, 0x6"))
              # Ganti Nama
              str_p = "workdir/res/values/strings.xml"
              if os.path.exists(str_p):
                  with open(str_p, "r", encoding="utf-8") as f:
                      d = f.read()
                  d = re.sub(r'(<string name="app_name">)(.*?)(</string>)', r'\1${{ github.event.inputs.mod_name }}\3', d)
                  with open(str_p, "w", encoding="utf-8") as f:
                      f.write(d)
          patch()
          EOF
          python3 patcher.py

      - name: Rebuild & Sign
        run: |
          apktool b workdir -o output/unsigned.apk
          keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          jarsigner -keystore debug.keystore -storepass android output/unsigned.apk androiddebugkey
          mv output/unsigned.apk output/GCamGo_Drpto_Patched.apk

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: GCamGo-Final
          path: output/GCamGo_Drpto_Patched.apk
          
