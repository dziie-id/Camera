name: Build Aperture Camera Signed

on:
  workflow_dispatch:
    inputs:
      package_name:
        description: 'Package Name'
        required: true
        default: 'com.aperture.standalone'
        type: string

jobs:
  build_process:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. SETUP JDK
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 2. SETUP STRUKTUR OUTPUT
      - name: Setup Output Folder
        run: |
          mkdir -p fixed_aperture_build/{aux_camera_configs,signing_configs,output}

      # 3. GENERATE AUX CONFIG (Untuk Multi-Lensa Sapphire)
      - name: Inject Sapphire AUX Config
        run: |
          cat > fixed_aperture_build/aux_camera_configs/camera_config_sapphire.xml <<EOF
          <?xml version="1.0" encoding="utf-8"?>
          <camera-config>
            <sensors>
              <sensor id="0" type="back" name="Main" />
              <sensor id="1" type="front" name="Front" />
              <sensor id="2" type="aux" name="Macro" />
              <sensor id="3" type="aux" name="UltraWide" />
            </sensors>
          </camera-config>
          EOF

      # 4. BYPASS/RESET KEYSTORE (Menghapus file lama jika ada)
      - name: Generate Keystore
        run: |
          KEYSTORE_FILE="fixed_aperture_build/signing_configs/release.keystore"
          
          # Hapus file keystore jika sudah ada agar tidak terjadi error alias sudah ada
          if [ -f "$KEYSTORE_FILE" ]; then
            echo "Keystore ditemukan, menghapus untuk membuat ulang..."
            rm -f "$KEYSTORE_FILE"
          fi
          
          keytool -genkey -v -keystore "$KEYSTORE_FILE" \
            -alias aperture_key -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Aperture, OU=Dev, O=Standalone, L=Jakarta, ST=Jakarta, C=ID" \
            -storepass password123 -keypass password123
          echo "✓ Keystore berhasil dibuat ulang tanpa error."

      # 5. BUILD APK DARI FOLDER SOURCE
      - name: Build Signed APK
        run: |
          # Masuk ke folder source sesuai struktur repo Anda
          cd source
          
          if [ -f "./gradlew" ]; then
            chmod +x gradlew
            echo "✓ gradlew ditemukan. Memulai build..."
            
            # Menjalankan build APK tipe Release
            ./gradlew assembleRelease
            
            # Pindahkan file APK ke folder output di root
            find . -name "*-release.apk" -exec cp {} ../fixed_aperture_build/output/aperture-sapphire-signed.apk \;
            echo "✓ Build sukses. APK tersedia di output."
          else
            echo "❌ ERROR: gradlew TIDAK ditemukan di folder source!"
            ls -R source/ # Debugging untuk melihat struktur jika gagal
            exit 1
          fi

      # 6. PUSH HASIL KE REPOSITORY
      - name: Push Results to Repository
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add fixed_aperture_build/
          # Commit hanya jika ada perubahan file
          git commit -m "Build: Signed APK & Configs Sapphire [skip ci]" || echo "Tidak ada perubahan."
          git push origin main
